/**
 * Shared API Module
 * 
 * Contiene utilidades y configuraciones compartidas para todas las llamadas API,
 * incluyendo cliente HTTP, interceptores y gestión de errores.
 * 
 * @module shared/api
 */

export { default as client } from './client';
export * from './types';
